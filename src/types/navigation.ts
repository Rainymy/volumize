export enum NavigationType {
    HOME = "/",
    MAIN = "/main",
    SETTINGS = "/settings",
}

export enum ConnectionState {
    CONNECTED = "connected",
    LOADING = "loading",
    DISCONNECTED = "disconnected",
}
